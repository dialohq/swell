// `@dialo/swell` runtime — scaffold placeholder.
//
// This file's substantive content lands in the runtime PR (q1). The
// scaffold PR (q0) only sets up the package shape (package.json,
// tsconfig, publish workflow) so subsequent PRs can layer the
// `q()` SQL marker + pg type augmentation onto a wired stack.

export {};
